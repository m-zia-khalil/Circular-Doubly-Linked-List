# Circular-Doubly-Linked-List
This project implements a circular doubly linked list in C++. It supports insertion at beginning and end, deletion from both ends, searching, and forward/backward traversal. Each node connects in both directions forming a circular structure, demonstrating dynamic memory management and advanced pointer manipulation in data structures.
